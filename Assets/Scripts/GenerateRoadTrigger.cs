using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GenerateRoadTrigger : MonoBehaviour
{
    // private void OnCollisionEnter(Collision col)
    // {
    //     if(col.gameObject.TryGetComponent<Player>(out Player player))
    //     {
    //         Vector3 genPos = new Vector3(0, 0, player.transform.position.z + 195);
    //         FindObjectOfType<RoadGenerator>().GenerateManually(genPos);
    //     }
    // }
}
